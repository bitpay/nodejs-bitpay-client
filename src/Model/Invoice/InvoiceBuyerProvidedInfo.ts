export interface InvoiceBuyerProvidedInfo {
  name: string;
  phoneNumber: string;
  sms: string;
  smsVerified: boolean;
  selectedTransactionCurrency: string;
  emailAddress: string;
  selectedWallet: string;
}
